query GetUserDetails($id: String!) {
  user(userId: $id) {
    id
    email
    displayName
    firstName
    lastName
    avatar
    creationDate
    modificationDate
    uuid
    groups {
      id
      displayName
    }
  }
}
